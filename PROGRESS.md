# Build progress

Tracks phase-by-phase status against Section 16 of `PROJECT_SPEC.md`. Update this file (not just chat) whenever a phase starts/completes, so progress survives a context reset.

## Post-Phase-7 polish (user-requested)

- Merged the standalone Why EMC page's 8 pillars into About Us, as a new section directly under Core Values (`id="why-emc"`, styled with `PillarCard` — `ValueCard`'s shell + heading color, plus the icon badges the pillars already had). Removed `/[locale]/why-emc`, `content/pages/why-emc.json`, `content/schemas/why-emc-page.ts`, and all references (nav, sitemap, `content.ts`); home page's Why EMC teaser now links to `/about#why-emc`. See DECISIONS.md for the design reasoning.

## Phase 1 — Foundation ✅ complete

- Repo scaffold: Next.js 15.5.23 (App Router, TS), Tailwind v4, shadcn/ui (Base UI variant), Framer Motion, Lucide, next-intl, Zod.
- Design tokens: Section 6.1 palette wired into Tailwind `@theme`; Manrope/Inter/IBM Plex Sans Arabic fonts; favicon/app-icon set generated from the supplied logo raster.
- i18n routing: `/en` + `/ar` via next-intl, full RTL mirroring verified live in-browser.
- Shared layout: header (sticky, condensing, mega-menu), mobile nav (Sheet), footer, WhatsApp FAB, back-to-top, language switcher, Request-Demo modal stub, search stub.
- Content layer: Zod schemas for all Section 8 models + typed getters (`src/lib/content.ts`); `stats.json` seeded `verified: false`; `testimonials/` empty.
- Verified: `npm run build` and `npm run lint` clean; visually checked in-browser (EN + AR, mega-menu, language switch, modal).

## Phase 2 — Home + About + Why EMC ✅ complete

- Resolved the Section 9.3/16 solution-count discrepancy (4 substantiated / 2 "expanding", following 9.3's specific reasoning — see DECISIONS.md) and seeded all 6 `content/solutions/*.json` + all 5 `content/partners/*.json` files as a prerequisite for the homepage grid/band (full detail-page content still lands in Phase 3).
- Added `content/schemas/{home-page,about-page,why-emc-page}.ts` + `content/pages/*.json` for one-off page copy that isn't a Section 8 model, with typed getters in `src/lib/content.ts`.
- Built shared components: `SolutionCard`, `PartnerTile`, `StatCounter` (scroll-triggered, `prefers-reduced-motion`-aware, unused until a verified stat exists), `CtaBand`, `SectionHeading`, `GradientMesh` (duotone editorial visual standing in for photography, per Section 6.1's guidance not to flat-fill hero backgrounds with purple), `Reveal`/`RevealGroup` (Framer Motion scroll-reveal per Section 6.4), `PageBreadcrumb` (RTL-mirrored chevron).
- **Home** (`/[locale]`): hero, trust band, 6-card solutions grid, UE Airway Platform featured block (verified UE Medical facts only), 5-tile partners band, Why EMC teaser, final CTA. Stats band / Knowledge Center preview / testimonials all correctly render nothing — no verified stats, no seeded news, no testimonials yet.
- **About Us** (`/[locale]/about`): Who We Are, Our Story, Vision, Mission, 5 Core Values, Leadership (CEO only, initials avatar — no invented bio or photo), Corporate Philosophy.
- **Why EMC** (`/[locale]/why-emc`): 8 pillars, each grounded in Section 1's own positioning language rather than invented SLA/operating-model numbers.
- Fixed a Base UI "nativeButton" accessibility warning at the shared `Button` component level (defaults `nativeButton` to `false` whenever a `render` target is supplied) rather than patching every call site — see DECISIONS.md.
- Verified: `npm run build` and `npm run lint` clean (11 routes, EN+AR for all 3 pages); full visual QA in-browser for both locales including RTL mirroring, scroll-reveal motion, and console-error-free render; grepped `content/` and `src/` for "years of experience," "certified," "award," "exclusive" — both hits trace cleanly to Section 2.1/2.2 (the stats.json placeholder label, and Long Life Surgical Industries' own verified ISO 13485 certification).

## Phase 3 — Solutions + Partners detail pages ✅ complete

- Added `content/schemas/index-pages.ts` + `content/pages/index-pages.json` for the Solutions/Partners index intros and a shared `detailPageCta` reused by every detail page (avoids repeating an identical CTA block across 11 content files).
- **Solutions index** (`/[locale]/solutions`): intro + full 6-card grid, reusing `SolutionCard` from Phase 2.
- **Solution detail** (`/[locale]/solutions/[slug]`, statically generated for all 6 slugs): hero, clinical overview, related-partner callout (only rendered when `relatedPartnerSlugs` is non-empty), related-products section (honest "coming soon" — Phase 4 seeds products), closing CTA. The 2 "expanding" solutions (Critical Care, Respiratory Care) automatically get the honest minimal state — no partner callout, no products section, no "Request a Demo" button, just the hero + disclaimer + CTA — verified live in-browser.
- **Partners index** (`/[locale]/partners`): intro + premium wordmark-tile grid of all 5 partners.
- **Partner detail** (`/[locale]/partners/[slug]`, statically generated for all 5 slugs): country/founded meta line, company summary, technology expertise, relationship statement (non-exclusivity default), clinical-area badges linking back to solution pages, featured-products honest "coming soon" state, external-website link (conditionally rendered — none of the 5 partners has a verified URL yet, so it doesn't show for any of them today).
- Extended `GradientMesh` to accept children (used for the partner-name overlay treatment on partner hero visuals, since no partner logos were supplied).
- Verified: `npm run build` (37 routes, all SSG) and `npm run lint` clean; grepped for "years of experience," "certified," "award," "exclusive" — same two known-clean hits as Phase 2; full in-browser QA of both locales, including the expanding-solution honest state and RTL mirroring on index/detail pages.

## Phase 4 — Products catalog ✅ complete

- Seeded UE Medical's actual product line as the reference implementation (Section 16): `uescope-video-laryngoscope`, `sacovlm-video-laryngeal-mask`, `flexible-bronchoscope`. Cross-checked against `reference/UE-Product-Presentation-EMC-V3.pdf`'s text layer (25 pages, extracted via `pdf-parse` since no PDF rendering tool was available) — confirmed everything in Section 2.1 and added SaCoVLM's fuller component list. Did not fabricate numeric specs the deck's graphic-only detail slides couldn't be extracted from (see DECISIONS.md); `technicalSpecs` for UEScope only carries Section 2.1's already-verified VL300/VL310 facts, SaCoVLM/Bronchoscope ship with a one-line compatibility spec only.
- Retroactively wired the new product data into Phase 2/3's placeholder sections: `airway-management`/`anesthesia` solutions' `relatedProductSlugs`, `ue-medical` partner's `featuredProductSlugs` — their detail pages now render real `ProductCard` grids instead of "coming soon."
- **Products index** (`/[locale]/products`): client-side `ProductCatalog` component with a Fuse.js search box, Solution filter, Manufacturer filter (Base UI `Select`), all working correctly against the 3 seeded products, verified live (filtering to a partner with no products correctly shows "No products match your filters").
- **Product detail** (`/[locale]/products/[slug]`, statically generated for all 3 slugs): hero with manufacturer byline, Request Demo/Speak with Specialist/Download Brochure (conditional) CTAs, Overview (structured category/family/model/business-unit — Section 8.3 has no dedicated overview text field, see DECISIONS.md), Clinical Applications, Key Benefits, Features, Technical Specifications (real `<table>` on desktop, `Accordion` on mobile per Section 9.4), Certificates, Related Products, Manufacturer info card, closing CTA. Accessories/Downloads/Videos sections are omitted entirely when empty (all 3 products today) rather than showing repeated "coming soon" blocks. No cart, no price, no "buy" language anywhere.
- Fixed a Base UI `Select` bug caught in QA: `SelectValue` doesn't auto-resolve an item's label from `value` alone — it needs a `children` render-prop — so both filter dropdowns were showing the raw value string ("all") instead of a translated label ("View all"). Fixed once in `ProductCatalog`, worth remembering for any future `Select` usage.
- Verified: `npm run build` (45 routes, all SSG) and `npm run lint` clean; grepped for the Section 18 fabrication-risk words — same two known-clean hits; full in-browser QA of both locales via a stable `next start` production server (the long-running `next dev` session had become unstable after ~25 minutes of live-editing this session — HMR kept rebuilding without new edits and the page eventually served stale/partial content; switched to `next start` for QA rather than chase the dev-server flakiness further, and confirmed the underlying DOM/data was always correct throughout).

## Phase 5 — Lead generation ✅ complete

- **`src/lib/leads/dispatch.ts`**: single `submitLead()` entry point fanning out to (1) durable local storage (`data/leads/<type>.jsonl`), (2) an internal notification email to EMC's inbox, (3) a confirmation email to the submitter — email is skipped gracefully (not a crash) when `RESEND_API_KEY` isn't set, verified live. Documented `.env.example` and the Phase-2 CRM/Odoo webhook extension point in code comments.
- **4 API routes**, each Zod-validating against the Section 8.8 schemas before calling `submitLead()`: `/api/leads/demo-request`, `/api/leads/quote-request`, `/api/leads/contact`, `/api/careers/apply` (schema-complete; no UI yet since the Careers page is Phase 6).
- **Demo Request modal**: replaced the Phase 1 stub with a real `react-hook-form` + Zod form (all 8 fields + optional message), inline success state, loading state, wired into the header (always visible per Section 11) plus the Home hero, and every "expanding"-excluded Solution/Product detail page CTA with a `defaultInterest` prefill.
- **Quotation Request modal**: new component, same pattern, wired into every `CtaBand`'s "Request a Quotation" button sitewide (product pages prefill the product name).
- **Contact page** (`/contact`): inquiry-type selector + form (Section 8.8's fixed 5-field shape), address-based Google Maps embed (no fabricated coordinates), phone/email/WhatsApp/socials, the spec's own unverified working-hours placeholder shown with a "pending confirmation" badge.
- Two Base UI bugs caught in QA and fixed at the pattern level (see DECISIONS.md for detail): (1) a `ReactElement` passed through a component prop and used as a `render` target silently drops its children — fixed by having both modals construct their trigger `<Button>` inline from primitive `variant`/`size` props instead of accepting a pre-built element; (2) three more bare `<SelectValue />` instances (missed in the Phase 4 sweep) were showing raw untranslated enum values instead of labels — fixed with the same children-render-prop pattern.
- Verified: `npm run build` (51 routes) and `npm run lint` clean; full end-to-end submission test for all 3 live forms (Contact, Demo Request, Quotation Request) in the browser — confirmed inline success states, no page navigation, and durable local JSONL writes for each; confirmed graceful no-crash behavior with `RESEND_API_KEY` unset; RTL-checked the Contact page and its now-correctly-translated Select dropdown.

## Phase 6 — Services + Knowledge Center + Careers ✅ complete

- **Services** (`/services`): all 9 services from Section 9.7 with distinct icons; the two spec-called-out CTAs ("Technical Support," "Tender Support") deep-link into `/contact?type=...`, verified live. "Tender Support" resolves to Contact's "Sales" inquiry type since Section 9.10 has no literal tender option (see DECISIONS.md).
- **Contact page prefill**: now reads a `?type=` query param and preselects the inquiry-type dropdown accordingly — the mechanism the Services CTAs (and anything else later) rely on.
- **Knowledge Center** (`/knowledge-center` + `/knowledge-center/[slug]`): `ArticleCard` shared component, type filter + search (Fuse.js) built and functional, but — since `content/news/` is still genuinely empty per Section 9.8 ("do not fabricate clinical articles") — the index renders an honest "Content is on its way" empty state instead of a non-functional filter bar. Detail template is ready the moment EMC supplies content.
- **Careers** (`/careers` + `/careers/[slug]`): empty-state listing with the spec's exact copy ("No open positions right now"), a generic culture section that reframes the same 5 verified values from an employee angle (no invented perks), job detail template, and a working Talent Network application form.
- **Talent Network application form** is the first form using real multipart file upload (not JSON) — `/api/careers/apply` now parses `FormData`, validates the CV's MIME type and 5MB size limit, and saves it to `data/uploads/cv/` (gitignored, same local-storage durability pattern and Vercel-ephemeral caveat as lead JSONL records). Verified end-to-end in-browser: submitted a real test PDF, confirmed both the lead record and the file landed on disk.
- Fixed two schema gaps found while wiring the form: `careersApplicationSchema.position` was `min(1)`-required despite Section 8.8 explicitly allowing "or 'general interest'" — now optional, defaulting server-side; `linkedinUrl`'s `.url().optional()` rejected react-hook-form's empty-string default (form inputs submit `""`, not `undefined`) — fixed with a `.refine()` that treats blank as not-provided without breaking the resolver's type inference (a `z.preprocess` version compiled but broke `zodResolver`'s generic types).
- Verified: `npm run build` (57 routes) and `npm run lint` clean; grepped for the Section 18 fabrication-risk words — same 3 known-clean hits, nothing new; full bilingual in-browser QA of all 3 new sections plus the Contact prefill, including the live file-upload test.

## Phase 7 — Legal, SEO, accessibility, performance, tests ✅ complete

- **Legal pages** (`/privacy-policy`, `/terms-of-use`, `/cookie-policy`): shared `LegalPageView` renderer, all 3 shipped as clearly-flagged generic drafts with a permanent on-page legal-review notice (Section 9.11 asks for this explicitly). Cookie Policy verified accurate by reading next-intl's routing source directly — only `NEXT_LOCALE` is set, no analytics/marketing cookies exist to claim.
- **SEO infrastructure** (Section 12): `src/lib/seo.ts` — `buildMetadata()` (canonical + hreflang en/ar/x-default + OpenGraph + Twitter card) now wired into every page's `generateMetadata`, description sourced from each page's own already-verified content field via `truncateDescription()`. `Organization` JSON-LD (home), `Product` JSON-LD (product detail, no price/offers), `Article` JSON-LD (Knowledge Center detail), `BreadcrumbList` JSON-LD (every interior page, injected once from `PageBreadcrumb`). `app/sitemap.ts` (all static + content-driven routes, both locales) and `app/robots.ts` (disallow `/api/`) added. `NEXT_PUBLIC_SITE_URL` documented in `.env.example`, defaults to `localhost:3000` — no domain was invented. Verified against a production server: sitemap.xml lists all 27 real routes, robots.txt correct, canonical/hreflang/OG/JSON-LD all render correctly in both locales.
- **Playwright test suite** (Section 4): `playwright.config.ts` + `e2e/` (navigation, language-switch, demo-form, accessibility — 18 tests total, all passing). Runs against a production build (`next build && next start`), not `next dev`, after `next dev`'s known Fast Refresh instability (see Phase 4) intermittently swallowed nav-link clicks during test runs. `npm run test:e2e` to run locally. axe-core (`@axe-core/playwright`) scans home (EN+AR), product detail, solutions index, contact, careers, and the open Request Demo modal against WCAG 2.1 A/AA — found and fixed one real remaining contrast gap: `--emc-teal-700` passed 4.5:1 on white but only 4.38:1 on `--emc-gray-50` (the site's alternating-section background), now `#277d76` (was `#28817a`), clearing both.
- **Performance pass** (Section 13): `npm run build` succeeds cleanly end-to-end (65 static pages, all routes). Verified the real production (`next start`) network waterfall is lean (2 CSS, ~13 split JS chunks, 2 fonts, 1 optimized image request, no third-party scripts). Confirmed font-display swap + next/font fallback metrics, image priority/CLS handling, lazy-loaded map iframe, and viewport-triggered (not eager) scroll animations were all already correctly in place from earlier phases. Could not get an actual Lighthouse score — no network access in this environment to install the CLI — a real Chrome DevTools/CI Lighthouse run is still recommended before launch.
- **Accessibility pass** (Section 13): found and fixed a real WCAG AA contrast failure the spec itself introduced — the brief's suggested "safe" teal for text only measures 3.66:1 on white (needs 4.5:1). Added `--emc-teal-700` (`#277d76`, tuned to also clear 4.5:1 on `--emc-gray-50`) and repointed every text (non-icon) usage of teal-600 to it (26 lines, 22 files), plus the global `--ring` focus token. Found and fixed a near-invisible keyboard focus ring on the mega-menu specifically (2:1, the one component without a solid `border-ring` layer alongside the soft glow) — verified live via real keyboard Tab, screenshot confirmed a crisp visible ring. Wired `aria-invalid`/`aria-describedby` from form errors onto their inputs. Confirmed Base UI's Dialog already satisfies focus-trap/Escape/return-focus/background-`aria-hidden` with no custom code needed. A real axe-core scan (via the Playwright suite above) later caught one more contrast gap the manual pass missed — see the Playwright entry.
- Also fixed during verification: `PageBreadcrumb`'s `<ol>` wrapped each trail entry in a `<span className="contents">` for React keys — invalid list markup axe correctly flagged (`<ol>`/`<ul>` must only contain `<li>`) even though it was visually invisible via `display:contents`. Fixed by using a `Fragment` instead, which renders no DOM element at all.
- **Full verification** (Task #44): clean `rm -rf .next && npm run build` succeeds (65 routes, lint+typecheck clean as part of the build), `npx tsc --noEmit` and `eslint .` both clean standalone, all 18 Playwright/axe tests pass against the production build, sitemap/robots/breadcrumb-markup/JSON-LD spot-checked directly against a running `next start` server, and the Section 18 fabrication-risk word grep across every file touched this phase turned up only the same already-verified ISO 13485 hit from earlier phases — nothing new fabricated.

## Phase 8 — not started

Lead-gen forms (Request Demo modal + Contact page + all four flows wired to email), Services/Knowledge Center/Careers, legal/SEO/a11y/performance passes, launch checklist. See `PROJECT_SPEC.md` Section 16 for full scope.

Known small polish items carried forward (see DECISIONS.md): `Partner.country` isn't localized (shows "China"/"United Kingdom" in Arabic pages too).
